document.content.map((string, index) => 
		if (string.indexOf(titre) === 0) {
		<div classNames'input-sm-title' id=index >string.slice('=')</div>		
		}
		else if (string.indexOf(sous-titre) === 0) {
		<input placeholder='titre d' classNames'input-sm-title'>		
		}


const sidebar = () =>{
		document.content.map((string, index) =>
         if (string.indexOf(titre) === 0) {     
         <a classNames'sidebar-chapter' href=index>string.slice('=')</a>                         }    
}
)
